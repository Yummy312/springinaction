<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
 http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

    <bean id="counter" class="com.springinaction.springidol.Counter"/>


<!--    В языке SpEL (Spring Expression Language) можно использовать операторы сравнения,-->
<!--    такие как в языке Java (например, <, >, <=, >=, ==, !=). Однако при использовании этих операторов-->
<!--    в конфигурационных XML-файлах могут возникать проблемы, так как XML имеет свои собственные правила синтаксиса,-->
<!--    и некоторые символы операторов, такие как < и >, имеют специальное значение в XML-->
<!--    (например, < используется для обозначения начала тега).-->

<!--    Чтобы избежать конфликтов в XML, рекомендуется использовать текстовые эквиваленты операторов SpEL:-->

<!--    lt вместо < (less than — меньше),-->
<!--    gt вместо > (greater than — больше),-->
<!--    le вместо <= (less than or equal to — меньше или равно),-->
<!--    ge вместо >= (greater than or equal to — больше или равно),-->
<!--    eq вместо == (equal — равно),-->
<!--    ne вместо != (not equal — не равно).-->
    <bean id="someBean" class="com.springinaction.springidol.SomeClass">
        <property name="hasCapacity" value="#{counter.total lt 100}"/>
    </bean>


</beans>