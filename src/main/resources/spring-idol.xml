<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
 http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

    <bean id="counter" class="com.springinaction.springidol.Counter"/>
    <bean id="circle" class="com.springinaction.springidol.Circle"/>


<!--    Язык SpEL поддерживает все основные арифметические операторы, имеющиеся в языке Java, -->
<!--    плюс оператор «крышки» (^), выполняющий возведение в степень-->

    <bean id="someBean" class="com.springinaction.springidol.SomeClass">
<!--        Например, чтобы сложить два числа, можно воспользоваться оператором +, как показано ниже-->
        <property name="adjustedAmount" value="#{counter.total + 42}"/>

<!--        Оператор * – умножение:-->
        <property name="circumference" value="#{ 2 * T(java.lang.Math).PI * circle.radius}"/>

<!--        Оператор / – деление:-->
        <property name="average" value="#{counter.total / counter.count}"/>

<!--        И оператор % – деление по модулю:-->
        <property name="remainder" value="#{counter.total % counter.count}"/>

<!--        В отличие от Java, язык SpEL также предлагает оператор возведения в степень:-->
        <property name="area" value="#{T(java.lang.Math).PI * circle.radius ^ 2}"/>

    </bean>


</beans>