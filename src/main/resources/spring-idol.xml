<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
 http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

<!--    Создание выражений, манипулирующих другими компонентами, – отличное начало. -->
<!--    Но что, если потребуется вызвать статический метод класса или получить значение константы? -->
<!--    Для этого необходимо познакомиться с особенностями использования типов-->
<!--    в языке SpEL-->

<!--   ___ Использование типов ___-->
<!--    Ключом к использованию методов класса и констант в языке-->
<!--    SpEL является оператор T() .-->


    <bean id="someObject1" class="com.springinaction.springidol.SomeClass" >

        <!--    Например, чтобы обратиться к Java классу Math в выражении на языке SpEL,-->
        <!--    необходимо использовать оператор T(). Предположим, что в свойство компонента необходимо-->
        <!--    внедрить значение pi. В этом случае достаточно просто обратиться-->
        <!--    к константе PI в классе Math, как показано ниже-->
        <property name="multiplier" value="#{ T(java.lang.Math).PI }"/>
    </bean>


    <bean id="someObject2" class="com.springinaction.springidol.SomeClass" >

<!--        Аналогично с помощью оператора T() можно вызывать статические методы. -->
<!--        Например, ниже показано, как в свойство компонента внедрить случайное число (в диапазоне от 0 до 1)-->
        <property name="multiplier" value="#{ T(java.lang.Math).random() }"/>
    </bean>

</beans>